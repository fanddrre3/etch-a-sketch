function createGrid() {
    const container = document.querySelector('.container');
    container.innerHTML = '';
    const gridSize = prompt('Enter number of squares per side (max 100):');
    const squareSize = 100 /gridSize;

    for (let i = 0; i < gridSize * gridSize)
}